import 'package:flutter/material.dart';

class Ichimlikpage extends StatelessWidget {
  const Ichimlikpage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Ichimlik'),),
    );
  }
}
